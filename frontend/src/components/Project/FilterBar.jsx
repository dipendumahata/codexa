const FilterBar = () => {
  return (
    <div className="filter-bar">
      <span>Repositories</span>
      <span>| Topics</span>
      <span>| Users</span>
      <span className="sort-dropdown">Sort ▾</span>
    </div>
  );
};

export default FilterBar;